# shared-dto

This library provides shared DTO contract types used by both API and POS for the sync boundary.

## Running unit tests

Run `nx test shared-dto` to execute the unit tests via [Vitest](https://vitest.dev/).
